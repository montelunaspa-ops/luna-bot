import fs from "fs";
import fetch from "node-fetch";

export async function transcribirAudio(url) {
  // Aquí puedes usar tu método para convertir audio a texto
  // Por ejemplo usando OpenAI Whisper
  // Por ahora simulamos:
  return "Transcripción de audio simulada";
}
